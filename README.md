# Negative

```bash
./negative -h
Usage:
    -f, --filename [FILENAME]        Input filename
    -o, --output [FILENAME]          Ouput filename
    -z, --[no-]reader                Include reader
```
